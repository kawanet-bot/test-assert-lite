// Fixture for builder/pack/Makefile's test-cli: a file the CLI can load
// that passes, to check the exit code on the success path end to end.
import {test} from "test-assert-lite"

test("passes", () => {})
